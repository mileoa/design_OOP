# Рефлексия
В примере по python сказано верно и действительно можно не создавать отдельный класс. Также есть нюанс как и сказано в статье в том что при антоации типов мы не получим от анализатора ошибку присываивания void(null) переменной тип для которой хотим иметь не void(null).